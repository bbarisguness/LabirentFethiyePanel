// project import
import defaults from './defaults';
import facilities from './facilities';
import settings from './settings';

const menuItems = {
  items: [defaults, facilities, settings]
};

export default menuItems;
